# 浪浪虎 · 产品需求文档

> 版本：v2.0 | 日期：2026-06-13 | 负责人：产品通

---

## 1. 产品概述

### 1.1 一句话定位

**浪浪虎**是一个从口头出行意图到可部署交互地图的端到端结构化流水线。用户只需要描述路线想法，系统自动完成路线研究、地图构建、UI 定制和网页部署。

### 1.2 解决的问题

| 用户痛点 | 浪浪虎的解决方式 |
|---------|---------------|
| 路线规划信息碎片化（天气、签证、路况分散在不同平台） | 阶段一统一搜索并结构化输出 YAML |
| 做地图需要编程技能（Leaflet/Mapbox/高德 API） | 阶段二全自动脚本构建，OSRM 拉取真实道路曲线 |
| UI 细节反反复复调整（图例颜色、线型虚实、图标尺寸） | 阶段三基于规则库自动注入，设计系统管住所有视觉决策 |
| 地图做完了不知道怎么发出去给旅伴看 | 阶段四 CloudStudio 一键部署，生成可分享链接 |

### 1.3 典型场景

| 场景 | 输入 | 输出 |
|------|------|------|
| 境内多方案自驾 | "暑假想从北京自驾东北边境线" | G331 环线交互地图（草原线/中心线二选一 + 返程 + 口岸筛选） |
| 出境自驾 | "想开车去贝加尔湖" | 贝加尔湖自驾地图（去程 + 东西线回程 + 飞航段 + 口岸标注） |
| 骑行/徒步路线 | "骑行川藏线" | 沿线补给节点 + 高海拔预警路段 + 休整城市 |
| 长距离摩托车 | "摩托车环中国海岸线" | 多段路线 + 稀疏加油站标注 + 路况警告 |
| 飞航结合 | "去勘察加看熊，中间飞过去" | 自驾 + 轮渡 + 飞航段混合路线，颜色区分交通方式 |

---

## 2. 目标用户

### 2.1 核心用户画像

| 用户类型 | 特征 | 核心诉求 |
|---------|------|---------|
| 自驾旅行策划者 | 有驾照、会做行程规划、需要在出发前可视化 | 一份能发给同行伙伴的网页地图，图例清晰 |
| 户外探险领队 | 需要标注风险点、补给站、逃生路线 | 路况三角警告 + 节点筛选 + 关键 GPS 坐标 |
| 旅行博主 / 内容创作者 | 需要美观的路线图嵌入文章 | 设计系统统一、颜色有语义、截图即用 |

### 2.2 非目标用户

- **纯导航需求**：已有高德/百度地图实时导航，浪浪虎不做 turn-by-turn 导航
- **公共交通规划**：不做火车/大巴时刻表，只做自驾/骑行/徒步
- **商业地图制作工具**：不做图层编辑、自定义样式、多人协作

---

## 3. 核心工作流：四阶段流水线

```
用户口述意图 → ① 路线规划 → ② 地图构建 → ③ UI 注入 → [预览确认] → ④ 部署上线
  自由描述    YAML 约束     脚本自动化     规则库自动化    人工确认      一键部署
```

### 3.1 阶段一：路线规划（判断密集）

**输入**：用户口头描述出行意图

**输出**：符合强制约束的 YAML 配置文件

**步骤**：
1. 解析用户意图 — 出发地/目的地/时间/出行方式/是否出境
2. 检索政策法规 — 出境游需确认签证、驾驶证互认、过境手续
3. 分析天气季节 — 按出发月份查询沿途气候、路况（冻土/雨季/台风季）
4. 识别关键节点 — 按 18 类节点分类体系标注
5. 方案对比 — 「快↔慢 × 省心↔冒险」十字架矩阵
6. 输出 YAML — 结构化记录路线、节点、警告信息

**强制字段约束**：YAML 不可缺省 `scenario.type/departure/season/vehicle`，每条 route 必须有 `id/name/style/color`，每个 stop 必须有 `name/lat/lng/node_type`，风险节点必须有 `alerts` 数组。

### 3.2 阶段二：地图构建（全自动）

**输入**：阶段一 YAML

**输出**：Leaflet HTML 骨架（坐标硬编码 + polyline 渲染）

**流程**：YAML 解析途经坐标 → OSRM 逐段拉取真实道路曲线 → 脚本生成 HTML

**技术栈**：Leaflet.js + OpenStreetMap + OSRM（Open Source Routing Machine）

### 3.3 阶段三：UI 定制（全自动规则注入）

**输入**：阶段二 HTML 骨架

**输出**：带完整 UI 组件的可预览 HTML

按 8 项强制清单逐项注入 UI 组件（详见 §5 UI 组件库）。

**阶段三检查点**：全部注入完成后必须 `preview_url` 打开本地 HTML 给用户确认——色虚实、图例、菱形、缩放、筛选、路况。用户确认后进入阶段四。

### 3.4 阶段四：部署（确认后执行）

部署前向用户展示概要 → 用户确认 → CloudStudio 一键部署 → 打开预览链接验证。

可选：本地打包为 `.html` 文件传到桌面。

---

## 4. 设计系统：线型 × 颜色

路线的视觉表达由两个**独立维度**决定，两者正交，不可混淆。

### 4.1 维度一：线型 = 路线确定性

| 路线角色 | 线型 | dashArray | weight |
|---------|------|-----------|--------|
| 唯一路线（无替代方案） | 实线 | 无 | 3.5 |
| 可选方案（多选一） | 点状虚线 | `"2, 5"` | 2.5 |
| 飞航段/轮渡 | 长虚线 | `"6, 8"` | 1.5 |

> 线型与方向无关。去程、返程都可以是实线或虚线，取决于是否存在替代路线。

### 4.2 维度二：颜色 = 方向/角色

| 路线方向 | 色值 | 色名 |
|---------|------|------|
| 去程首选 | `#7c3aed` | 紫色 |
| 去程备选 | `#2563eb` | 蓝色 |
| 回程首选 | `#DB536A` | 珊瑚 |
| 回程备选 | `#d97706` | 琥珀 |
| 飞航/轮渡 | `#0891b2` | 青色 |
| 终点标记 | `#059669` | 绿色 |

### 4.3 辅助色

| 用途 | 色值 | 色名 |
|------|------|------|
| 极值节点（北极/东极） | `#06b6d4` | 青蓝 |
| 路况警告 | `#eab308` | 黄色 (fill-opacity: 0.65) |
| 途经城市 | `#9ca3af` | 灰色 |

### 4.4 节点形状约定

| 节点类型 | 形状 | 尺寸 | 说明 |
|---------|------|------|------|
| 起点/终点 | 大圆 ● | radius 8 | circleMarker + addLabel |
| 口岸/关键城市 | 菱形 ◆ | 15px SVG | 纯色无文字，bindTooltip |
| 途经城市 | 小圆 ● | radius 6 | 灰色 fill，白色边框 |
| 路况警告 | 三角 ▲ | 14px SVG | 半透明黄，fill-opacity: 0.65 |

### 4.5 城市标注规则

- 以城市为主体，不用景点名或路段名
- 相邻城市合并，不重复标注
- 特征城市用 `城市 · 特征` 格式（如「抚远 · 中国东极」）
- 极值点用鲜明色 `#06b6d4`，起点用路线主色

---

## 5. UI 组件库（8 项）

> 以下 8 项均为强制规则，阶段三必须逐项注入，不得跳过。

| # | 组件 | 关键规格 |
|---|------|---------|
| 1 | 缩放控制 | zoomSnap 0.2 / wheelPxPerZoomLevel 300 / 手动 L.control.zoom |
| 2 | 底部图例栏 | flex 横向分组 + `margin: 0 auto 2px auto` + left padding 4px |
| 3 | 口岸筛选器 | checkbox toggle，勾选聚焦口岸并隐藏路线 |
| 4 | 飞航段/轮渡 | 青色长虚线 `"6, 8"`，opacity 0.7 |
| 5 | 路况标记 | 半透明黄三角 ▲，仅标注需注意路段 |
| 6 | 菱形图标 | 15px SVG 纯菱形 + 白边 + drop-shadow |
| 7 | 起点/终点 | radius 8 circleMarker + static-label 文字 |
| 8 | 城市标注 | 城市名 + 特征后缀 + 颜色规则 |

---

## 6. 关键节点分类体系

```
关键节点（Key Node）
│
├── 起止点 ── start / end
├── 边境节点 ── border_out / border_in / border_transit（出境游激活）
├── 补给休整 ── fuel / rest / repair（长距离路线激活）
├── 交通切换 ── ferry / flight / trailhead
├── 景观兴趣 ── scenic / cultural / event（可选）
└── 风险节点 ── danger / police / altitude（建议标注）
```

---

## 7. 已交付案例

| 案例 | 类型 | 路线特征 | 部署地址 |
|------|------|---------|---------|
| 贝加尔湖自驾 | 出境 | 去程实线 + 东西线虚线 + 满洲里↔伊尔库茨克飞航段 | CloudStudio |
| G331 东北边境环线 | 境内 | 草原线/中心线虚线可选 + 返程珊瑚实线 + 口岸筛选 + 北极/东极标注 | CloudStudio |

两个案例覆盖了：境内↔出境、单一↔多方案、口岸筛选、飞航段、路况三角、菱形标注、极值点突出等全部能力。

---

## 8. 技术架构

| 层级 | 技术 | 用途 |
|------|------|------|
| 地图渲染 | Leaflet.js 1.9.4 | 矢量图层、标记、弹窗、缩放 |
| 底图 | OpenStreetMap | 免费瓦片，全球覆盖 |
| 路线坐标 | OSRM API | 逐段拉取真实道路曲线（非直线） |
| 部署 | CloudStudio + codebuddy.work | 一键部署，免认证，可分享链接 |
| 配置输入 | YAML | 结构化路线描述，版本 2 强制字段约束 |
| 输出 | 单文件 HTML | 零依赖部署，拷贝即用 |

---

## 9. 安全合规

```yaml
部署前自动检查:
  - 手机号 (11 位数字模式)
  - 身份证号 (18 位数字模式)
  - 邮箱地址
  - 真实姓名
  - 家庭住址
  # 地理坐标和旅行提示为公开信息，不含个人隐私
```

---

## 10. 产品边界（Non-Goals）

| 不做的事 | 原因 |
|---------|------|
| 实时导航 / turn-by-turn | 已有高德/百度地图，不与导航产品竞争 |
| 公共交通规划 | 聚焦自驾/骑行/徒步场景 |
| 多人协作编辑地图 | 当前定位为个人规划工具 |
| 商业 API 底图（Google Maps） | OSM 免费且全球覆盖，降低使用门槛 |
| PDF / 图片导出 | 网页可截图，但不做导出引擎 |
| 数据持久化后端 | YAML + HTML 本地文件即产品交付物 |

---

## 11. 版本历史

| 版本 | 日期 | 关键变更 |
|------|------|---------|
| v1.0 | 2026-06-05 | 首个版本，贝加尔湖 Case 1 |
| v1.1 | 2026-06-10 | 新增 G331 案例、UI 组件库初步成型 |
| v2.0 | 2026-06-13 | 结构化流水线重构：线型×颜色双维度设计系统、阶段三规则注入、阶段四确认流程、菱形图标统一 15px、图例 dot 规范 |

---

> 本文档由产品通基于浪浪虎 SKILL.md v2.0 整理。
> 仓库: https://github.com/Ninawang07/langlang-tiger
